# Dokploy 部署环境变量配置示例
# 复制此文件并根据你的实际需求修改配置

# ===========================================
# 基础配置
# ===========================================

# Web 终端服务端口
# 默认: 7681
PORT=7681

# 时区设置
# 可选值: Asia/Shanghai, UTC, America/New_York 等
TIMEZONE=Asia/Shanghai

# Node.js 运行环境
# 可选值: development, production, test
NODE_ENV=production

# ===========================================
# 存储配置
# ===========================================

# 工作空间数据存储路径
# 这是在 Dokploy 宿主机上的实际路径
# 建议使用 Dokploy 标准数据目录
WORKSPACE_PATH=/etc/dokploy/data/claude-terminal/workspace

# ===========================================
# Claude Code 配置
# ===========================================

# 如果你有 Claude Code API 密钥，可以在这里配置
# ANTHROPIC_API_KEY=your_api_key_here

# Claude Code 配置文件路径（容器内）
# CLAUDE_CONFIG_PATH=/root/.claude

# ===========================================
# 安全配置
# ===========================================

# ttyd 相关安全配置（可选）
# 如果需要认证，可以设置用户名和密码
# TTYD_USERNAME=admin
# TTYD_PASSWORD=secure_password

# 是否启用 SSL（由 Dokploy 反向代理处理）
# SSL_ENABLED=false

# ===========================================
# 性能配置
# ===========================================

# 容器资源限制（可选，由 Dokploy 管理）
# MEMORY_LIMIT=512m
# CPU_LIMIT=0.5

# tmux 会话配置
TMUX_SESSION_NAME=claude_session

# ===========================================
# 日志配置
# ===========================================

# 日志级别
# 可选值: debug, info, warn, error
LOG_LEVEL=info

# 是否启用详细日志
VERBOSE_LOGS=false

# ===========================================
# 网络配置
# ===========================================

# 容器网络名称
NETWORK_NAME=claude-terminal-network

# 健康检查配置
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3

# ===========================================
# Dokploy 特定配置
# ===========================================

# Dokploy 项目标签
DOKPLOY_PROJECT=claude-terminal

# 自动重启策略
RESTART_POLICY=unless-stopped

# 容器名称
CONTAINER_NAME=claude-terminal

# ===========================================
# 高级配置
# ===========================================

# 如果需要自定义 ttyd 启动参数
# TTYD_EXTRA_ARGS=--readonly-paths /etc

# 如果需要挂载额外的目录
# EXTRA_VOLUMES=/host/path:/container/path

# 自定义启动命令（高级用户）
# CUSTOM_CMD=ttyd -i 0.0.0.0 -p 7681 tmux new -A -s claude_session

# ===========================================
# 使用说明
# ===========================================

# 1. 复制此文件为 .env
# 2. 根据需要修改配置值
# 3. 在 Dokploy 界面中设置这些环境变量
# 4. 部署应用

# 注意事项:
# - 密码和 API 密钥等敏感信息请妥善保管
# - 路径配置请确保在 Dokploy 宿主机上存在且有正确权限
# - 端口配置请确保不与其他服务冲突
# - 时区设置会影响日志时间和计划任务